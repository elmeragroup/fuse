# Implementation Plans

Generated by the improve skill on 2026-08-21 (branch audit of `feat/theme-cleanup`).
Execute in the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Plans were written against commit `85aa892`. Each plan's drift check lists its in-scope
paths — run it before executing.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Split `theme-api.browser.test.tsx` into three focused browser-test files | P1 | M | — | DONE — advisor-verified 2026-08-21 against working tree at `85aa892`: all done criteria re-run green; 62 tests preserved across 510/539/220-line files |
| 002 | Small review fixes: honest test tripwires, BrandLogo guard, doc wording, pin-check dedup | P2 | S | — | DONE — advisor-verified 2026-08-21 against working tree at `85aa892`: all done criteria re-run green; fresh browser run 63/63, none skipped |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- None. 001 and 002 touch disjoint files (`theme-api.browser.test.tsx` + new test files vs
  `turbo.json`, `brand-logo.tsx`, `validate-theme.ts`, scratch doc) and can run in either
  order or in parallel. Note both plans add browser tests to the same package, so whoever
  lands second should re-check the other's count expectation: 001 pins the baseline at
  62; 002 expects 62 → 63.

## Findings considered and rejected

From the branch audit (2026-08-21). Recorded so they aren't re-audited:

- **`turbo lint` depends on `@elmeragroup/ui#build`** — deliberate and documented
  (`turbo.json` comment, theming.md §7.7); verdict was "revisit if lint latency bites."
  Not a finding.
- **`demo-stage-comfortable.css` export resolves to `dist`** — same trade as the
  pre-existing `themes.css` export; `turbo dev` already depends on builds.
- **`themeAxisDeps` optional-axis reads / `NestedThemeValidator` hook-order shape** —
  documented design (ADR 0003, theming.md §7.6): validation must throw only after hooks
  register.
- **Unescaped attribute interpolation in `applyHostRootAttributes`** — inputs are
  compile-time internal constants in a build-time Vite adapter; not attacker-reachable.
- **`LEGAL_THEMES` type precision trade** (`as const` tuple → `readonly ThemeInput[]`) —
  conscious decision noted in the thermo-nuclear review; nothing consumes literal element
  types.
