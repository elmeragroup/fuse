# Implementation Plans

Generated by the improve skill on 2026-08-26 from branch commit `7cbf25f`. Execute in the order below unless dependencies say otherwise. Each executor must read its plan fully before starting, honor its STOP conditions, run every verification gate, and update its row when done.

## Execution order & status

| Plan                                                          | Title                                                        | Priority | Effort | Depends on | Status                                                         |
| ------------------------------------------------------------- | ------------------------------------------------------------ | -------- | ------ | ---------- | -------------------------------------------------------------- |
| [001](001-preserve-overlay-ref-containers.md)                 | Preserve explicit ref containers until Base UI resolves them | P1       | S      | —          | BLOCKED (Base UI portals never-attached refs to document.body) |
| [002](002-protect-tooltip-description-link.md)                | Keep the Tooltip's accessible description link intact        | P1       | S      | 001        | BLOCKED (depends on 001)                                       |
| [003](003-make-generated-artifact-checks-non-self-healing.md) | Make generated-artifact checks observe drift before repair   | P1       | M      | —          | DONE                                                           |
| [004](004-make-docs-readiness-claims-truthful.md)             | Make release and visual-regression readiness claims truthful | P2       | S      | —          | DONE                                                           |
| [005](005-run-anti-slop-rule-tests-in-ci.md)                  | Run the vendored anti-slop rule tests in CI                  | P2       | S      | —          | DONE                                                           |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002 requires 001 because both modify `packages/ui/src/components/tooltip/tooltip.tsx` and its browser tests. Completing the portal-container lifecycle fix first keeps the accessibility plan's drift check and regression edits narrow.
- 003, 004, and 005 are independent of the overlay work and of one another. They may be assigned separately, but their executors must not edit outside each plan's scope.

## Findings considered and rejected

- Docs tests might read an unbuilt application: rejected because `apps/docs/turbo.json` already makes `test` depend on `build`; the selected generated-artifact problem is the duplicate generation inside that ordered graph, not missing build order.
- Docs raw-HTML and repository-path reads might be injection or traversal risks: rejected because the inputs are repository-controlled generated/spec content rather than untrusted request data; no exploitable boundary was found.
- `git diff --check` whitespace findings: rejected because the matches are intentional vendored/license/path data and the repository's normative `pnpm format:check` passes.

## Shared executor conventions

- Use Node 24, pnpm 11, Turbo, Vitest, Oxlint, and Oxfmt as already configured; do not introduce alternatives unless a plan explicitly says so.
- Use conventional commits matching recent history (`fix(ui): ...`, `refactor(docs): ...`, `test(lint): ...`).
- `@elmeragroup/ui` user-facing fixes require a changeset; internal docs/tooling changes use the `no-changeset` PR label.
- Do not push, open a PR, or broaden scope without operator instruction.
