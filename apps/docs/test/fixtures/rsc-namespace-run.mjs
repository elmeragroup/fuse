/**
 * Entry for the server-component fixture. Node loads this file, then the loader
 * evaluates the TypeScript server module under the `react-server` condition.
 */
await import("./rsc-namespace-server.ts");
