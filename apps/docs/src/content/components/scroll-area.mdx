---
title: ScrollArea
lede: >
  Styled custom scrollbars over native scrolling. `ScrollArea.Root` ships one bar for
  its orientation; the viewport stays a real scroll container.
export: ScrollArea
source: packages/ui/src/components/scroll-area/scroll-area.tsx
demos:
  - id: vertical
    title: Vertical
    file: scroll-area-vertical.tsx
  - id: horizontal
    title: Horizontal
    file: scroll-area-horizontal.tsx
  - id: always-visible
    title: Always visible
    file: scroll-area-always.tsx
  - id: composed
    title: Two-axis composition
    file: scroll-area-composed.tsx
---

## Composition limits

`Root` renders exactly one scrollbar. For two-axis scrolling, compose the base-ui
primitives directly and mount two `ScrollArea.Bar`s — the last demo shows that escape
hatch.
