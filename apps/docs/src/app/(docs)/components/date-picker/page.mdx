---
lede: >
  A date field with a calendar popover.
---

import { DatePickerBasic } from "./demos/date-picker-basic";
import { DatePickerControlled } from "./demos/date-picker-controlled";
import { DatePickerPresets } from "./demos/date-picker-presets";
import { DatePickerValidation } from "./demos/date-picker-validation";
import { DatePickerStates } from "./demos/date-picker-states";
import { DatePickerInModal } from "./demos/date-picker-in-modal";

import { componentMetadata } from "../../../../lib/component-page";

export const metadata = componentMetadata("date-picker");

<ComponentIntro slug="date-picker" />

<Prose>

Values are `CalendarDate` objects from `@internationalized/date`, which you install separately; the library does not re-export it.

Wrap locale-dependent usage in `UiProviders` so it follows the app locale.

</Prose>

<Demo slug="date-picker" id="basic" title="Basic" file="date-picker-basic.tsx">
  <DatePickerBasic />
</Demo>

<Demo slug="date-picker" id="controlled" title="Controlled" file="date-picker-controlled.tsx">
  <DatePickerControlled />
</Demo>

Presets stack above the calendar on small screens. A popup with limited vertical space scrolls to keep every date reachable.

<Demo slug="date-picker" id="presets" title="Presets" file="date-picker-presets.tsx">
  <DatePickerPresets />
</Demo>

<Demo slug="date-picker" id="validation" title="Validation" file="date-picker-validation.tsx">
  <DatePickerValidation />
</Demo>

<Demo slug="date-picker" id="states" title="States" file="date-picker-states.tsx">
  <DatePickerStates />
</Demo>

<Demo slug="date-picker" id="in-modal" title="Inside a dialog" file="date-picker-in-modal.tsx">
  <DatePickerInModal />
</Demo>

<ApiReference slug="date-picker" />

<ComponentTokens slug="date-picker" />
