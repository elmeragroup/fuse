// Source entry facade for `@elmeragroup/ui/checkbox-card` (Appendix A). Pure re-export file:
// explicit named re-exports only — no `export *`, no local declarations, no directives.
// The exports/barrel generators discover this file; never hand-edit package.json#exports
// or src/index.ts. No public recipe.
export { CheckboxCard } from "./components/checkbox-card/checkbox-card";
export type { CheckboxCardProps } from "./components/checkbox-card/checkbox-card";
