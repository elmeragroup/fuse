// Source entry facade for `@elmeragroup/ui/phone-number-field` (Appendix A). Pure re-export file:
// explicit named re-exports only — no `export *`, no local declarations, no directives.
// The exports/barrel generators discover this file; never hand-edit package.json#exports
// or src/index.ts. No public recipe — layout comes from FieldFrame (phone-number-field.md §4).
export { PhoneNumberField } from "./components/phone-number-field/phone-number-field";
export type { PhoneNumberFieldProps } from "./components/phone-number-field/phone-number-field";
