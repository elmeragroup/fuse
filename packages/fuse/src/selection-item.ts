// Source entry facade for `@elmeragroup/fuse/selection-item` (Appendix A). Pure re-export file:
// explicit named re-exports only — no `export *`, no local declarations, no directives.
// The exports/barrel generators discover this file; never hand-edit package.json#exports
// or src/index.ts. The outline recipe is borrowed from the item entry, not re-exported here.
export { SelectionItem } from "./components/selection-item/selection-item";
