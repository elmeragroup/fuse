// Source entry facade for `@elmeragroup/fuse/combobox` (Appendix A). Pure re-export file:
// explicit named re-exports only — no `export *`, no local declarations, no directives.
// The exports/barrel generators discover this file; never hand-edit package.json#exports
// or src/index.ts. No public recipe — all styling is inline per part.
export { Combobox } from "./components/combobox";
export { useComboboxAnchor } from "./components/combobox/combobox";
export type {
  ComboboxChipProps,
  ComboboxClearProps,
  ComboboxContentProps,
  ComboboxInputProps,
  ComboboxRootProps,
} from "./components/combobox/combobox";
