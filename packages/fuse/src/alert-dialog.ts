// Source entry facade for `@elmeragroup/fuse/alert-dialog` (Appendix A). Pure re-export file:
// explicit named re-exports only — no `export *`, no local declarations, no directives.
// The exports/barrel generators discover this file; never hand-edit package.json#exports
// or src/index.ts.
export { AlertDialog } from "./components/alert-dialog";
export type { AlertDialogContentProps } from "./components/alert-dialog";
