// Source entry facade for `@elmeragroup/fuse/span` (Appendix A). Pure re-export file:
// explicit named re-exports only — no `export *`, no local declarations, no directives.
// The exports/barrel generators discover this file; never hand-edit package.json#exports
// or src/index.ts. `spanVariants` is PUBLIC.
export { Span } from "./components/span/span";
export type { SpanProps } from "./components/span/span";
export { spanVariants } from "./components/span/span-variants";
