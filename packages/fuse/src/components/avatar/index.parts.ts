import { AvatarRoot, AvatarGroup, AvatarImage, AvatarFallback } from "./avatar";

export const Root = AvatarRoot;
export const Group = AvatarGroup;
export const Image = AvatarImage;
export const Fallback = AvatarFallback;
