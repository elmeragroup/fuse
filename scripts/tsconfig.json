{
  "extends": "@elmeragroup/typescript-config/internal-package.json",
  "compilerOptions": {
    "noEmit": true,
    // The release entry point imports packages/ui/scripts/release-pack.ts, so the program root is the checkout.
    "rootDir": ".."
  },
  "include": ["**/*.ts"]
}
