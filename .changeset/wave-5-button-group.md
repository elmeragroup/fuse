---
"@elmeragroup/ui": minor
---

Ship ButtonGroup — a client `role="group"` composite that collapses inner radii and borders of `data-slot` children, with public `buttonGroupVariants` and a `useRender` Text part.
