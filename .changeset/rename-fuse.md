---
"@elmeragroup/fuse": minor
---

Name the shared component library Fuse. Install and import `@elmeragroup/fuse`, and use `FuseProvider` and `useFuse` from `@elmeragroup/fuse/theme` for locale context. The workspace package now lives in `packages/fuse`.
