---
"@elmeragroup/ui": minor
---

Ship Span — de-RAC'd inline typography primitive with the public `spanVariants` recipe (extends `textVariants`, `leading` default `snug`) and `render` polymorphism via base-ui `useRender`.
