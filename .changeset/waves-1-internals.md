---
"@elmeragroup/ui": minor
---

Generate the root barrel and runtime export names from entry facades, add package-private localized strings, enforce the react-aria import quarantine, and replace the size-limit stub with measured gzip ceilings.
