---
"@elmeragroup/ui": minor
---

Ship Show — a server-safe render helper with no DOM of its own (`when ? <>{children}</> : null`). Children are eagerly evaluated; there is no lazy render-prop or `fallback`.
