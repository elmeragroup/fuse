---
"@elmeragroup/ui": minor
---

Ship Meter — labeled read-only base-ui meter composite with provider locale and mode × level colors — and the `METER_CONSTANTS` export naming the four modes and four levels.
