---
"@elmeragroup/ui": minor
---

Add the quarantined `@elmeragroup/ui/react-aria/focusable` entry — a verbatim RAC `Focusable` / `useFocusable` re-export. Migration debt that dies with the react-aria tier.
