---
"@elmeragroup/ui": minor
---

Ship Code — a server-safe syntax-highlighted block over `sugar-high`, with `data-slot="code"` and a keyboard-reachable `role="region"` scroll container. Raw source only; `highlight()` escapes token text.
