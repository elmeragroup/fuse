---
"@elmeragroup/ui": minor
---

Ship Tabs — a client namespace (`Root`, `List`, `Trigger`, `Content`) over `@base-ui/react/tabs` with both list variants and both orientations, and the public `tabsListVariants` recipe.
