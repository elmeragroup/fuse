---
"@elmeragroup/ui": minor
---

Add the quarantined `@elmeragroup/ui/react-aria/date-picker` entry — an interim labeled date picker composing the public `DateInput` and `Calendar` in a contained popover, plus `DatePickerPresetGroup` / `DatePickerPresetItem`.
