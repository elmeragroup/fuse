---
"@elmeragroup/ui": patch
---

Test support only: `CONTROL_SM` is exported from the shared themed-browser-render module alongside `CONTROL_MD`, so the signed `sm` control rung is written down once. No runtime code changes.
