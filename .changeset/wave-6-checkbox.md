---
"@elmeragroup/ui": minor
---

Ship Checkbox — the base-ui primitive, labeled `CheckboxGroup` / `CheckboxItemGroup`, `CheckboxItem` row (aliasing `SelectionItem` parts by identity), and `CheckboxDescription`.
