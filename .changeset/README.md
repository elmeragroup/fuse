# Changesets

User-facing PRs add a changeset (`pnpm changeset`). Internal-only PRs (CI, docs site, tests) use the `no-changeset` GitHub label instead. Merging to `main` opens a Version Packages PR; publishing is a later workflow.
