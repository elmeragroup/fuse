---
"@elmeragroup/ui": minor
---

Ship Card — eight-part server-safe surface namespace with the public `cardVariants` slot recipe (single `direction` axis) that TextField's card variant and CheckboxCard compose.
