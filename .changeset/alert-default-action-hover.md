---
"@elmeragroup/fuse": patch
---

`Alert.Root`: the default variant's action button hovers to the `muted` fill instead of Button's
translucent primary fill, which left its `foreground` label at low contrast while hovered.
