---
"@elmeragroup/ui": patch
---

Test support only: `roleNamed`, `headingNamed` and `cssVarColor` are exported from the shared themed-browser-render module, and the helper-coverage walkers now cover the react-aria tier as well as `components/`.
