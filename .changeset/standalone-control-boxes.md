---
"@elmeragroup/ui": patch
---

Correct Input, Textarea, button and composite field sizing in standalone CSS consumers without changing the host page's reset. Dense and comfortable controls now match their documented border-box dimensions.
