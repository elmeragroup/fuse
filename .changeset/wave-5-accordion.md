---
"@elmeragroup/ui": minor
---

Ship Accordion — a client namespace (`Root`, `Item`, `Header`, `Trigger`, `Content`) reimplementing the radix accordion API shape on `@base-ui/react/accordion`, with the public `accordionVariants` recipe, a Phosphor `CaretDown` chevron, and a plain height transition against `--accordion-panel-height`.
