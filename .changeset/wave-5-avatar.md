---
"@elmeragroup/ui": minor
---

Ship Avatar — a client base-ui passthrough namespace (`Root`, `Image`, `Fallback`) with `data-slot` on every part and a tokenized `bg-muted text-muted-foreground` fallback surface.
