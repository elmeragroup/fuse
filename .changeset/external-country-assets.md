---
"@elmeragroup/ui": patch
---

Keep country flags as external SVG assets in default Vite production builds. Dynamic flag and phone-field consumers now load only selected images instead of embedding flag artwork in JavaScript.
