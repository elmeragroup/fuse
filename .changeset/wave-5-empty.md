---
"@elmeragroup/ui": minor
---

Ship Empty — a server-safe empty-state namespace (`Root`, `Header`, `Media`, `Title`, `Description`, `Content`) whose Description renders a `p` and whose Media slot is `empty-media`.
