---
"@elmeragroup/ui": minor
---

Ship Sidebar — the application shell at `@elmeragroup/ui/sidebar`: `Sidebar.Provider` plus 23 parts, `useSidebar`, and the documented constants (`SIDEBAR_COOKIE_NAME = "sidebar:state"`, widths, cmd/ctrl+B shortcut). Desktop gap/fixed-container rail with `offcanvas`/`icon`/`none` collapse modes, mobile `Sheet` branch below 768px, collapsed icon-mode `Tooltip` labels, cookie-persisted open state, and `sidebar.*` dictionary strings in all four locales.
