---
"@elmeragroup/ui": patch
---

Add the package-private overlay/field spine — `useResolvedPortalContainer`, `OverlayContainerProps`/`OverlayPositionerProps`, the overlay popup class constants, `selfFocusRingClass`/`withinFocusRingClass`, `isTextNode`, and `createStringDictionary` — and compose `Popover.Content` from it. No public export is added and Popover's props, classes, and DOM are unchanged.
