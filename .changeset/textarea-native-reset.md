---
"@elmeragroup/ui": patch
---

Synchronize the uncontrolled TextareaField character counter after native form reset, while preserving canceled resets, controlled values, and change callback behavior.
