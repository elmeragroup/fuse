---
"@elmeragroup/ui": minor
---

Ship RadioGroup — the labeled composite, `RadioGroupItem` primitive, compact `Radio` rows, `RadioItem` selection cards (aliasing `SelectionItem` parts by identity), `RadioItemGroup`, and `RadioIconButton`.
