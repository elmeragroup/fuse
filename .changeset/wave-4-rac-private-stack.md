---
"@elmeragroup/ui": patch
---

Add the package-private react-aria support stack (internal button/field/checkbox/dialog/modal/popover, overlay-container seam) and the react-aria dependency trio — no public API surface; the `react-aria/*` entries ship with the date cluster.
