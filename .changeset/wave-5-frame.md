---
"@elmeragroup/ui": minor
---

Ship Frame — a server-safe grouped-panel namespace (`Root`, `Panel`, `Header`, `Title`, `Description`, `Footer`) on a muted ground. Card owns the standalone-card look; the two compounds do not share an API.
