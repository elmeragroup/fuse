---
"@elmeragroup/ui": minor
---

Ship Select — a namespace over base-ui Select with a `data-size` trigger axis on `--control-*`, ThemeScope-contained portals via `container` on Content, and `alignItemWithTrigger` defaulting to true.
