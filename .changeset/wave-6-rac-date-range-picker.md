---
"@elmeragroup/ui": minor
---

Add the quarantined `@elmeragroup/ui/react-aria/date-range-picker` entry — an interim labeled date-range picker composing two public `DateInput` rows and the public `RangeCalendar` in a contained popover.
