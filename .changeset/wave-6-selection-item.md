---
"@elmeragroup/ui": minor
---

Ship SelectionItem — the shared card-row shell (`Shell`/`Title`/`Description`/`Content`/`Actions`/`SubSection`) with the new `controlPosition` axis that CheckboxItem and RadioItem will alias.
