---
"@elmeragroup/ui": minor
---

Ship TimelineList — a server-safe semantic ordered-list compound (`Root`, `Item`, `Title`, `Time`, `Description`) whose `Title` composes Heading and whose `Time` normalizes `Date` or string to ISO `dateTime`.
