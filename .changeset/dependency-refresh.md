---
"@elmeragroup/ui": patch
---

Refresh runtime dependencies to the latest admitted versions. Base UI is
1.8.0, React Aria is 3.52.1, and React Aria Components is 1.21.1; their
exact published pins move with the catalog. `@internationalized/date` is
3.12.4, `libphonenumber-js` is 1.13.12, and `sugar-high` is 2.4.0
(published range `^2.4.0`). The `code` entry's budget is recalibrated for
sugar-high v2's larger language set.
