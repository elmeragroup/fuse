{
  "name": "@elmeragroup/api-extractor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "node scripts/build.ts",
    "ci:checks": "pnpm run format:check && pnpm run lint && pnpm run build && pnpm run check:boundary && pnpm run type-check && pnpm run test:fixtures && pnpm run test && pnpm run test:timing",
    "format:check": "oxfmt --check .",
    "lint": "oxlint . --deny-warnings",
    "test": "vitest run",
    "test:timing": "node scripts/issue-02-timing.ts --check",
    "report:timing": "node scripts/issue-02-timing.ts --write",
    "check:boundary": "node scripts/check-boundary.ts",
    "test:fixtures": "tsc --project test/fixtures/issue-02-tsconfig.json --noEmit && tsc --project test/fixtures/issue-03-tsconfig.json --noEmit && tsc --project test/fixtures/issue-03-object-apis/tsconfig.json --noEmit && tsc --project test/fixtures/issue-03-review/tsconfig.json --noEmit && tsc --project test/fixtures/issue-03-review/MixedRepo/tsconfig.json --noEmit",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@elmeragroup/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "oxlint": "catalog:",
    "react": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
