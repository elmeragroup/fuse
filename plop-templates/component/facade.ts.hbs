// Source entry facade for `@elmeragroup/ui/{{name}}` (Appendix A). Pure re-export file:
// explicit named re-exports only — no `export *`, no local declarations, no directives.
// The exports/barrel generators discover this file; never hand-edit package.json#exports
// or src/index.ts. Add `{{camelCase name}}Variants` here only if it is intended for public reuse.
export { {{pascalCase name}} } from "./components/{{name}}/{{name}}";
export type { {{pascalCase name}}Props } from "./components/{{name}}/{{name}}";
